dev:
  port: 8000
  temp_dir: "./tmp"
  s3_config:
    endpoint: "http://localhost:9000"
    region: local
    bucket: test
    forcePathStyle: true
    credentials:
      accessKeyId: minio
      secretAccessKey: password
