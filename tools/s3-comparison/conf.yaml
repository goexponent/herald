primary:
  bucket_name: "<bucket_name>"
  credentials_file: "./credentials"
  profile: "primary"
  endpoint_url: "<endpoint_url>"

mirror:
  bucket_name: "<bucket_name>"
  credentials_file: "./credentials"
  profile: "mirror"
  endpoint_url: "<endpoint_url>"
